{% block choice_row %}
    <div class="cookie-consent__category">
        <div class="cookie-consent__category-toggle">
            {% for child in form %}
                {% if not child.rendered %}
                    {{- form_widget(child) -}}
                    {{- form_label(child, null, {translation_domain: choice_translation_domain}) -}}
                {% endif %}
            {% endfor %}
        </div>
        <div class="cookie-consent__category-information">
            <h4 class="cookie-consent__category-title">{{ ('cookie_consent.'~name~'.title')|trans({}, 'CookieConsentBundle') }}</h4>
            <p class="cookie-consent__category-description">{{ ('cookie_consent.'~name~'.description')|trans({}, 'CookieConsentBundle') }}</p>
        </div>
    </div>
{% endblock %}

{% block submit_row %}
    <div class="cookie-consent__btn-wrapper">
        {{ block('button_widget') }}
    </div>
{% endblock %}

